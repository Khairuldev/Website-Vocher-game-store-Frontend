import React from "react";

interface TopUpCardProps {
  value: string;
  category: string;
  price: string;
}

export default function TopUpCard(props: TopUpCardProps) {
  const { value, category, price } = props;
  return <></>;
}
